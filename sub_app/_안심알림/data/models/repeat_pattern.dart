// TODO: 반복 패턴 구현
// 1. RepeatType enum 정의 (none, daily, weekly, monthly, custom)
// 2. RepeatPattern 클래스 구현:
//    - 타입 필드 (RepeatType)
//    - 간격 필드 (int interval)
//    - 요일 필드 (List<int> daysOfWeek)
//    - 종료일 필드 (DateTime? endDate)
// 3. fromJson(), toJson() 메서드 구현
// 4. 다음 알림 시간 계산 메서드 구현 (getNextOccurrence(DateTime from))

enum RepeatType {
  // 여기에 반복 타입 나열
}

class RepeatPattern {
  // 여기에 필드 정의

  // 생성자 구현

  // 메서드 구현
}
